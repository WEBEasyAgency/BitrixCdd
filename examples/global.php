<?php
/**
 * Пример глобальной конфигурации
 *
 * Все значения мержатся в каждый конфиг инфоблока как базовые.
 * Конкретный инфоблок может переопределить любое значение.
 *
 * sync_mode:
 *   'off'    - структура раз на version, данные никогда
 *   'soft'   - структура раз на version, данные раз (create only)
 *   'ensure' - структура раз на version, данные: создать/дозаполнить пустое
 *   'once'   - полная синхронизация при смене version, потом стоп
 *   'danger' - полная синхронизация каждый хит (только для разработки)
 */
return [
    'sync_mode' => 'soft',

    // Настройки полей для CIBlock::SetFields (применяются ко ВСЕМ инфоблокам)
    'iblock_fields' => [
        'CODE' => [
            'IS_REQUIRED' => 'N',
            'DEFAULT_VALUE' => [
                'TRANSLITERATION' => 'Y',   // Автогенерация CODE из NAME
                'TRANS_LEN'       => 100,
                'TRANS_CASE'      => 'L',   // Нижний регистр
                'TRANS_SPACE'     => '-',
                'TRANS_OTHER'     => '-',
                'TRANS_EAT'       => 'Y',
                'USE_GOOGLE'      => 'N',
            ],
        ],
    ],
];
